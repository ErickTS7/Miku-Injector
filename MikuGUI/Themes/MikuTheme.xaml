<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Background geral da janela -->
    <SolidColorBrush x:Key="BgBrush" Color="#FF2AA8A0"/>

    <!-- Background de painéis / bordas -->
    <SolidColorBrush x:Key="PanelBrush" Color="#FF1F7F7C"/>

    <!-- Botão padrão -->
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#FF2AA8A0"/>
        <Setter Property="BorderBrush" Value="#FF1F7F7C"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- TextBox padrão -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#FF1F7F7C"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#FF2AA8A0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
    </Style>

</ResourceDictionary>
